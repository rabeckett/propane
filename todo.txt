always display the router name when possible, wrap this in a single function

Avoid duplicate constraints (which level?)
add syntax for building ranges of ASes (e.g., as[101-500])
Need better, router-level minimization
Concrete failure scenario for counterexample
Fix issue with MED, Prepending etc for Backbone example
Issue with avoid for external peers when going through out

Reuse finding path code across Regex and CGraph modules
Can we do a better job avoiding garbage states when building the automaton

add syntax + support for IP V6
add syntax + support for different prefix matches (e.g., pfx[8..24])
